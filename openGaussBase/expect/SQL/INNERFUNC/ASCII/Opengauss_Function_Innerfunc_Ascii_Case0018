

-- @testpoint: 转换两个字段数值

select ASCII(power(2,31),9E+10);
?.*ERROR: function ascii\(double precision, numeric\) does not exist
  Hint: No function matches the given name and argument types. You might need to add explicit type casts.
?.*
  Where: referenced column: ascii
