-- @testpoint: 输入参数数是0，合理报错
select concat();
?.*ERROR: function concat\(\) does not exist
  Hint: No function matches the given name and argument types. You might need to add explicit type casts.
?.*
  Where: referenced column: concat
